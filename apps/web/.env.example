# Base URL of the FastAPI backend (apps/api). NEXT_PUBLIC_ because both
# server components (page data fetching) and client components (form
# submits, review decisions, prompt edits) call it directly — see
# lib/api.ts. The backend has no auth yet, so this isn't a secret.
NEXT_PUBLIC_API_URL=http://localhost:8000
