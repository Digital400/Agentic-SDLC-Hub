// Agent prompt templates will live here once the agent engine (LangGraph)
// is introduced — e.g. draft/review/validate prompts per SDLC stage.
// Intentionally empty for the foundation step.

export {};
