// Shared TypeScript types/constants used by both the web app and future
// internal tooling (e.g. SDLC stage names, artifact types, API contracts).
// Intentionally empty for the foundation step.

export {};
